name: MinGW

on:
  push:
    branches:
      - '**'

permissions:
  contents: read

jobs:
  momo-test:
    strategy:
      matrix:
        include:

        # x64
        - arch: 'x64'
          build_type: 'Release'
          no_exceptions_rtti: 'Off'
          extra_settings: 'Off'
          flags: '-std=c++23 -Werror -Wno-error=maybe-uninitialized'

        - arch: 'x64'
          build_type: 'Release'
          no_exceptions_rtti: 'Off'
          extra_settings: 'Off'
          flags: '-std=c++26 -Werror -Wno-error=maybe-uninitialized'

        - arch: 'x64'
          build_type: 'Debug'
          no_exceptions_rtti: 'Off'
          extra_settings: 'Off'
          flags: '-std=c++26 -Werror'

        - arch: 'x64'
          build_type: 'Release'
          no_exceptions_rtti: 'On'
          extra_settings: 'Off'
          flags: '-std=c++26 -Werror -Wno-error=maybe-uninitialized'

        - arch: 'x64'
          build_type: 'Release'
          no_exceptions_rtti: 'Off'
          extra_settings: 'On'
          flags: '-std=c++26 -Werror -Wno-error=maybe-uninitialized'

        # x86
        - arch: 'x86'
          build_type: 'Release'
          no_exceptions_rtti: 'Off'
          extra_settings: 'Off'
          flags: '-std=c++23 -Werror -Wno-error=maybe-uninitialized'

        # GNU 16.1.0: libcxx20_tree_map_vp_access_at
#        - arch: 'x86'
#          build_type: 'Release'
#          no_exceptions_rtti: 'Off'
#          extra_settings: 'Off'
#          flags: '-std=c++26 -Werror -Wno-error=maybe-uninitialized'

        # Out of memory
#        - arch: 'x86'
#          build_type: 'Debug'
#          no_exceptions_rtti: 'Off'
#          extra_settings: 'Off'
#          flags: '-std=c++26 -Werror'

        - arch: 'x86'
          build_type: 'Release'
          no_exceptions_rtti: 'On'
          extra_settings: 'Off'
          flags: '-std=c++26 -Werror -Wno-error=maybe-uninitialized'

        - arch: 'x86'
          build_type: 'Release'
          no_exceptions_rtti: 'Off'
          extra_settings: 'On'
          flags: '-std=c++26 -Werror -Wno-error=maybe-uninitialized'

    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v6
#    - name: Install Ninja
#      run: choco install ninja
    - name: Setup MinGW
      uses: egor-tensin/setup-mingw@v3
      with:
        platform: ${{ matrix.arch }}
    - name: Build
      env:
        CXX: g++
        CXXFLAGS: ${{ matrix.flags }}
      run: |
        cd test
        mkdir build
        cd build
        cmake .. -G Ninja -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} `
          -DMOMO_TEST_NO_EXCEPTIONS_RTTI=${{ matrix.no_exceptions_rtti }} `
          -DMOMO_TEST_EXTRA_SETTINGS=${{ matrix.extra_settings }}
        cmake --build . -v
    - name: Test
      run: test/build/momo_test.exe
